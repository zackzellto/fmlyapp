import React from "react";
import { UserRegistrationForm } from "../components/Forms/Registration/RegistrationForm";

type Props = {};

const Register = (props: Props) => {
  return (
    <div>
      <UserRegistrationForm />
    </div>
  );
};

export default Register;
